# Last Commit · 早睡作息系统

一个帮人从高反馈的电脑工作里慢慢退出来的夜间工具。

它不会催你立刻关机。你先把今晚的时间安排好，把还惦记的事情留给明早，再顺着睡前步骤离开电脑。

## 在线体验

正式体验地址：https://last-commit-sleep.vercel.app

开源仓库：https://github.com/NovaKepler513/last-commit-sleep

当前已经公开，任何人打开链接都能直接进入，不需要 ChatGPT 登录，也不需要安装应用。

这一版已经可以完整使用时间规划、交接清单、睡前步骤和睡眠月历。v12.32 给四段稳定动作同时准备了桌面 WebM 和 iPhone/iPad MOV；如果浏览器仍然丢失透明通道，页面会自动使用静态猫，避免出现灰色方块。手机端的品牌标、三个时间名称和时间框也重新对齐。

页面右下角有一只很小的蝴蝶。点开它，可以看到作者署名、开源代码和四套主题猫的使用说明。

## 第一次怎么用

1. 点“进入今晚”，或者按 `Enter`。
2. 在“今晚怎么安排”里选择收工、入睡和起床时间。所有时间以 15 分钟为一档。
3. 调整“想睡多久”。点“按 X 小时重算”时，页面会从起床时间往前推算睡觉时间，再留出一小时收尾。
4. 在“明早先做什么”里写下今天停在哪里，并把明早要做的事逐条加入清单。
5. 点“写好了，开始收工”。睡前步骤会在下面展开。
6. 收好工作资料、做一点别的、洗漱、把手机放远。需要时可以跟着做三轮 4–2–6 呼吸：圆心倒数这一口还剩几秒，右侧正计时显示整段已经做了多久；中途可以暂停、继续或重来。
7. 点“今晚收好”。这晚会出现在睡眠月历里。

第二天早上，交接区会变成“看看昨晚留的事”。任务可以逐项完成；误清空的清单可以恢复。

## 页面里还有什么

- “专注”只保留收工倒计时和像素猫，适合把页面放在旁边看。
- 四种颜色主题会更换页面和空闲猫。当前通过审片的视频动作是伸懒腰、眨眼转耳、短暂打盹和听秒针。
- 点击、双击或长按猫，会得到不同回应。少数时候它会暂时抓住鼠标，按 `Esc` 可以提前取回。
- “留个备份”可以复制交接内容、导出 Markdown 或生成日历文件。
- “重置今晚”只重置当晚流程。

## 数据与隐私

时间、交接、清单和睡眠记录只保存在当前浏览器的 `localStorage` 中，不会上传到服务器。

这也意味着：

- 换浏览器或换设备后，数据不会自动同步；
- 清理浏览器站点数据可能会删除记录；
- 公开托管者无法看到你的交接内容。

它是一件生活节奏工具，不提供医疗建议，也不替代睡眠障碍的专业诊断。

## 下载后运行

项目没有构建步骤，也没有后端。

最简单的方式是直接打开：

```text
Last Commit·早睡作息系统.html
```

更稳定的方式是在项目目录启动静态服务器：

```bash
python3 -m http.server 8769 --bind 127.0.0.1
```

然后打开：

```text
http://127.0.0.1:8769/
```

根目录的 `index.html` 只是线上与本地静态服务器的入口转接，正式页面仍然使用项目名称命名。

## 公开发行包

```text
Last Commit·早睡作息系统.html   正式页面
index.html                     静态托管入口
assets/                        当前样式、交互脚本和四只主题猫
README.md                      使用说明
LICENSE                        页面代码的 MIT License
ASSET-LICENSE.md               猫咪与视觉资产的单独许可
NOTICE.md                      署名与权利拆分
asset-license.html             适合在浏览器阅读的视觉资产说明
notice.html                    适合在浏览器阅读的完整版权说明
PUBLIC_REFERENCES.md           可公开的参考资料
CONTRIBUTING.md                参与方式
```

内部项目母目录还保存历史版本、角色生成过程、失败样本和验收记录。这些材料用于复盘，不进入公开发行包。公开包由 `scripts/build-public-release.sh` 按白名单生成，避免把本地路径和内部过程一起上传。

## 研究与来源

公开资料见 `PUBLIC_REFERENCES.md`。项目母目录内部另有完整来源账，用于保存原始导出、睡眠依据、Web 标准、角色母图和设计过程。

原始 Fable 导出 ZIP 当前已经不在最初的下载路径；这件事也被登记为证据缺口。现存的 v1–v6 恢复文件保留了 SHA-256，不会用一条失效路径假装原件仍在。

## 部署

这是一个纯静态项目。当前通过 Vercel 托管，项目名为 `last-commit-sleep`，生产地址为 `https://last-commit-sleep.vercel.app`。GitHub 主分支已经连接 Vercel，新提交会触发自动发布。

如果改用 GitHub Pages、Cloudflare Pages 或其他静态托管服务：

1. 把本目录作为公开仓库根目录；
2. 不需要安装依赖，也不需要填写环境变量；
3. 发布目录选择仓库根目录；
4. 确认根网址打开的是 `index.html`；
5. 发布后把真实网址补到本 README 的“在线体验”。

## 当前动作进度

v12.32 撤下了 C05 边缘探看，因为旧片让猫用四只爪倒退，步态和关节都不自然。C01 轻微受惊、C03 碰爪、C08 按住鼠标也继续等待重做，旧片的问题是猫向镜头前冲时同时放大并漂移脸型。当前运行时只接入 C02、C04、C06、C07 四段已经通过的动作。

下一批先重做 C01、C03、C05、C08，再补自然走两步、真实回头、整理胡须和轻呼吸四段。生成提示词与审片材料保存在内部生产档案，不随公开仓库发布。新片仍需同时完成桌面和 iPhone 真机透明视频验收，审片通过后才接回网页。

## 许可

- 页面代码使用 MIT License，公开署名为 `Nova Kepler · 天与视界`，见 `LICENSE`。
- 角色与视觉包括四套主题猫，以及相关图片、动画、视频与宣传视觉；具体使用方式见 `ASSET-LICENSE.md`，浏览器阅读版见 `asset-license.html`。
- 完整署名和第三方资料边界见 `NOTICE.md`。

你可以按 MIT License 修改和再发布页面代码。四套主题猫和相关视觉素材有单独的使用说明，请在使用前阅读 `ASSET-LICENSE.md`。
